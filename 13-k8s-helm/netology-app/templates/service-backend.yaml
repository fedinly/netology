apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: helm
spec:
  selector:
    app: netology-app
  type: ClusterIP
  ports:
  - port: {{ .Values.service.backend.port }}
    name: multitool
    protocol: TCP
    targetPort: 8080
